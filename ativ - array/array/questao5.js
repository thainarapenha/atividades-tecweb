function impares(inicio = 0, fim = 100){
    let aux = inicio
    
    if(inicio > fim){
        inicio == fim
        fim = aux
    }

    for(let i = 0; i < fim; i++){
        i % 2 !== 0
        console.log(`${i}`)
    }
}